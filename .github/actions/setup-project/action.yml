name: Set up project
description: Provision the project's pinned Nub and Node.js toolchain.

runs:
  using: composite
  steps:
    - name: Set up Nub and Node.js
      uses: nubjs/setup-nub@v0
      with:
        package-manager-cache: false
