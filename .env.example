# Runtime scalar configuration is read by Alchemy and injected into the
# specific Worker that consumes it. Never put real secret values in this file.
BETTER_AUTH_SECRET=

# Add a model-provider API key supported by Pi.
ANTHROPIC_API_KEY=
